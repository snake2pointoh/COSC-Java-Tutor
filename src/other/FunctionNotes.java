package src.other;

import java.util.function.Function;

public class FunctionNotes {
    // predicate // takes in value, returns bool
    // function // takes in value, returns another value
    // consumer // takes in value, returns null
    // provider // returns value

    // biPredicate // takes in two values, returns bool
    // biFunction // takes in two values, returns value
    // biConsumer // takes in two values, returns null

    // construct function classes using lambda

    // look at function references using (::)
}
